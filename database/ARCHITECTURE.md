# Database Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         SUSPENSION PREDICTION SYSTEM                         │
│                      Database Architecture (Supabase)                        │
└─────────────────────────────────────────────────────────────────────────────┘


                    ┌──────────────────────────────────┐
                    │      GITHUB ACTIONS (Hourly)     │
                    │  ┌───────────────────────────┐   │
                    │  │  scripts/collect_and_log.py│  │
                    │  └───────────────────────────┘   │
                    └────────┬───────────┬─────────────┘
                             │           │
                ┌────────────┴───┐  ┌───┴──────────────┐
                │                │  │                   │
        ┌───────▼────────┐  ┌───▼──────────┐  ┌───────▼────────┐
        │  PAGASA Checker │  │ Open-Meteo   │  │ Prediction     │
        │  (Node.js +     │  │ Collector    │  │ Model          │
        │   Python)       │  │ (Python)     │  │ (Python)       │
        └────────┬────────┘  └──────┬───────┘  └───────┬────────┘
                 │                  │                   │
                 │                  │                   │
                 └──────────┬───────┴───────────────────┘
                            │
                  ┌─────────▼──────────┐
                  │  Supabase Client   │
                  │  (Python SDK)      │
                  └─────────┬──────────┘
                            │
                            │ service_role key
                            │ (write access)
                            │
        ┌───────────────────▼────────────────────────┐
        │                                            │
        │          SUPABASE (PostgreSQL)             │
        │                                            │
        │  ┌──────────────────────────────────────┐ │
        │  │  Tables (5)                          │ │
        │  │  ┌────────────────────────────────┐  │ │
        │  │  │ 1. daily_predictions           │  │ │
        │  │  │    - prediction_date, lgu      │  │ │
        │  │  │    - suspension_probability    │  │ │
        │  │  │    - predicted_suspended       │  │ │
        │  │  │    - actual_suspended          │  │ │
        │  │  │    - model_version             │  │ │
        │  │  └────────────────────────────────┘  │ │
        │  │                                       │ │
        │  │  ┌────────────────────────────────┐  │ │
        │  │  │ 2. weather_data                │  │ │
        │  │  │    - weather_date, lgu, type   │  │ │
        │  │  │    - precipitation_sum         │  │ │
        │  │  │    - temperature, wind, etc    │  │ │
        │  │  └────────────────────────────────┘  │ │
        │  │                                       │ │
        │  │  ┌────────────────────────────────┐  │ │
        │  │  │ 3. pagasa_status               │  │ │
        │  │  │    - has_active_typhoon        │  │ │
        │  │  │    - tcws_level                │  │ │
        │  │  │    - rainfall_warning_level    │  │ │
        │  │  └────────────────────────────────┘  │ │
        │  │                                       │ │
        │  │  ┌────────────────────────────────┐  │ │
        │  │  │ 4. collection_logs             │  │ │
        │  │  │    - run_date, run_time        │  │ │
        │  │  │    - success/failure tracking  │  │ │
        │  │  │    - error messages            │  │ │
        │  │  └────────────────────────────────┘  │ │
        │  │                                       │ │
        │  │  ┌────────────────────────────────┐  │ │
        │  │  │ 5. model_metadata              │  │ │
        │  │  │    - model_version             │  │ │
        │  │  │    - performance metrics       │  │ │
        │  │  │    - feature_importance        │  │ │
        │  │  └────────────────────────────────┘  │ │
        │  └──────────────────────────────────────┘ │
        │                                            │
        │  ┌──────────────────────────────────────┐ │
        │  │  Analytics Views (4)                 │ │
        │  │  - prediction_accuracy_by_lgu        │ │
        │  │  - weather_suspension_correlation    │ │
        │  │  - pagasa_impact_analysis            │ │
        │  │  - collection_reliability            │ │
        │  └──────────────────────────────────────┘ │
        │                                            │
        │  ┌──────────────────────────────────────┐ │
        │  │  Row Level Security (RLS)            │ │
        │  │  - Public read access (anon key)     │ │
        │  │  - Authenticated write (service key) │ │
        │  └──────────────────────────────────────┘ │
        │                                            │
        └─────────────────┬──────────────────────────┘
                          │
                          │ REST API
                          │ (anon key - read only)
                          │
        ┌─────────────────▼──────────────────────────┐
        │                                            │
        │      GITHUB PAGES DASHBOARD                │
        │      (JavaScript + HTML)                   │
        │                                            │
        │  ┌──────────────────────────────────────┐ │
        │  │  Dashboard Features:                 │ │
        │  │  - Latest predictions                │ │
        │  │  - Prediction accuracy charts        │ │
        │  │  - Weather correlation graphs        │ │
        │  │  - PAGASA impact analysis            │ │
        │  │  - Collection reliability stats      │ │
        │  └──────────────────────────────────────┘ │
        │                                            │
        └────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              DATA FLOW SUMMARY
═══════════════════════════════════════════════════════════════════════════════

1. COLLECTION (Every Hour via GitHub Actions)
   ┌─────────────────────────────────────────────────────────────────────────┐
   │ PAGASA Checker → scrape typhoon/rainfall data                          │
   │ Open-Meteo Collector → fetch weather forecast (17 LGUs, 13 variables)  │
   │ Prediction Model → generate suspension probabilities                   │
   └─────────────────────────────────────────────────────────────────────────┘

2. LOGGING (via Supabase Client)
   ┌─────────────────────────────────────────────────────────────────────────┐
   │ Insert pagasa_status (typhoon, TCWS, rainfall warnings)                │
   │ Insert weather_data (precipitation, wind, temperature, etc)            │
   │ Insert daily_predictions (probability, classification, model version)  │
   │ Insert collection_logs (success/failure, errors, duration)             │
   └─────────────────────────────────────────────────────────────────────────┘

3. STORAGE (Supabase PostgreSQL)
   ┌─────────────────────────────────────────────────────────────────────────┐
   │ Tables store raw data with proper types and constraints               │
   │ Views aggregate data for analytics (accuracy, correlation, etc)       │
   │ Indexes enable fast queries by date, LGU, status                      │
   │ Row Level Security allows public read, authenticated write            │
   └─────────────────────────────────────────────────────────────────────────┘

4. DASHBOARD ACCESS (GitHub Pages)
   ┌─────────────────────────────────────────────────────────────────────────┐
   │ Fetch data via REST API using anon key (read-only)                    │
   │ Display predictions, charts, and analytics                            │
   │ Real-time updates (data refreshes every hour)                         │
   │ No backend needed - direct API calls from JavaScript                  │
   └─────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                           SECURITY ARCHITECTURE
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────┐         ┌────────────────────────┐
│   GITHUB ACTIONS       │         │   GITHUB PAGES         │
│   (Write Operations)   │         │   (Read Operations)    │
├────────────────────────┤         ├────────────────────────┤
│ Env: SUPABASE_URL      │         │ JS: SUPABASE_URL       │
│ Env: SUPABASE_KEY      │         │ JS: SUPABASE_ANON_KEY  │
│                        │         │                        │
│ Key Type:              │         │ Key Type:              │
│ • service_role         │         │ • anon                 │
│ • Full access          │         │ • Read-only            │
│ • Bypasses RLS         │         │ • Respects RLS         │
│                        │         │                        │
│ Operations:            │         │ Operations:            │
│ • INSERT predictions   │         │ • SELECT predictions   │
│ • INSERT weather       │         │ • SELECT from views    │
│ • INSERT logs          │         │ • Display charts       │
│ • UPDATE outcomes      │         │ • Show analytics       │
└────────────────────────┘         └────────────────────────┘
         │                                  │
         └────────┬───────────────────────┬─┘
                  │                       │
         ┌────────▼───────────────────────▼────────┐
         │      SUPABASE ROW LEVEL SECURITY        │
         ├─────────────────────────────────────────┤
         │ Policies:                               │
         │ • Public SELECT: true (anyone can read) │
         │ • Service INSERT: authenticated only    │
         │ • Service UPDATE: authenticated only    │
         │                                         │
         │ Result:                                 │
         │ • Dashboard: read-only access ✓         │
         │ • GitHub Actions: full write access ✓   │
         │ • Unauthorized: read-only access ✓      │
         └─────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                           COST & CAPACITY ANALYSIS
═══════════════════════════════════════════════════════════════════════════════

FREE TIER LIMITS:
┌─────────────────────────────────────────────────────────────────────────────┐
│ Storage: 500 MB database                                                   │
│ Bandwidth: 2 GB/month                                                      │
│ API Requests: Unlimited                                                    │
│ Row Level Security: Included                                               │
│ Auto backups: Daily (7 days retention)                                     │
└─────────────────────────────────────────────────────────────────────────────┘

ESTIMATED USAGE (1 YEAR):
┌─────────────────────────────────────────────────────────────────────────────┐
│ Daily predictions:    17 LGUs × 365 days = 6,205 records    ≈ 1 MB       │
│ Weather data:         17 LGUs × 365 × 2 types = 12,410      ≈ 15 MB      │
│ PAGASA status:        24 checks/day × 365 = 8,760           ≈ 2 MB       │
│ Collection logs:      24 runs/day × 365 = 8,760             ≈ 1 MB       │
│ Model metadata:       ~10 versions                          ≈ 0.1 MB     │
│                                                                             │
│ TOTAL YEAR 1:                                                ~20 MB       │
│ FREE TIER:                                                   500 MB       │
│ CAPACITY USAGE:                                              4%          │
│                                                                             │
│ → Can run for 20+ years on free tier! 🎉                                   │
└─────────────────────────────────────────────────────────────────────────────┘

BANDWIDTH USAGE (MONTHLY):
┌─────────────────────────────────────────────────────────────────────────────┐
│ GitHub Actions writes: 24 runs × 30 days × 5 KB = 3.6 MB/month           │
│ Dashboard reads:       100 users × 10 visits × 50 KB = 50 MB/month       │
│                                                                             │
│ TOTAL MONTHLY:                                             ~54 MB         │
│ FREE TIER:                                                 2,000 MB       │
│ BANDWIDTH USAGE:                                           2.7%          │
│                                                                             │
│ → Well within limits! 🎉                                                    │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                            DEPLOYMENT CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

SETUP (5-10 minutes):
  ☐ 1. Create Supabase account at supabase.com
  ☐ 2. Create new project "suspension-predictions"
  ☐ 3. Choose Singapore region (closest to Philippines)
  ☐ 4. Run database/schema.sql in SQL Editor
  ☐ 5. Copy Project URL from Settings → API
  ☐ 6. Copy service_role key from Settings → API
  ☐ 7. Add SUPABASE_URL to GitHub Secrets
  ☐ 8. Add SUPABASE_KEY to GitHub Secrets
  ☐ 9. Install: pip install supabase
  ☐ 10. Test: python database/supabase_client.py

INTEGRATION (5-10 minutes):
  ☐ 11. Update GitHub Actions workflow with env vars
  ☐ 12. Add collection script to workflow
  ☐ 13. Test workflow with manual trigger
  ☐ 14. Verify data appears in Supabase dashboard
  ☐ 15. Update dashboard JavaScript with anon key
  ☐ 16. Test dashboard reads from database
  ☐ 17. Enable hourly cron schedule
  ☐ 18. Monitor first 24 hours of collection

VALIDATION:
  ☐ 19. Check collection_logs table for success rate
  ☐ 20. Verify predictions appear in dashboard
  ☐ 21. Test analytics views (accuracy, correlation, etc)
  ☐ 22. Confirm data retention within free tier limits

═══════════════════════════════════════════════════════════════════════════════

Files Created:
  • database/schema.sql              - PostgreSQL schema (570 lines)
  • database/supabase_client.py      - Python client (550 lines)
  • database/README.md               - Setup guide (450 lines)
  • scripts/collect_and_log.py       - Integration script (300 lines)
  • DATABASE_COMPLETE.md             - Summary document
  • requirements.txt                 - Updated with dependencies

Total Lines: ~2,000 lines of production-ready database code! 🚀
```
