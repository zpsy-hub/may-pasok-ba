# Requirements for Historical Data Backfill Scripts
# Install with: pip install -r backfill/requirements.txt

# HTTP requests
requests>=2.31.0

# Progress bars
tqdm>=4.66.0

# Data manipulation (already in main requirements, but listed for completeness)
pandas>=2.1.0
numpy>=1.24.0

# Machine learning (for prediction generation)
scikit-learn>=1.3.0
joblib>=1.3.0

# Database (already in main requirements)
supabase>=2.0.0
